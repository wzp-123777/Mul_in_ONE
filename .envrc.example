use flake

# Default Configuration for Mul-in-One
# These settings are read by src/mul_in_one_nemo/config.py

# Path to the persona definitions file #SaaS服务模式下已经不使用硬编码的yaml文件
export MUL_IN_ONE_PERSONAS="personas/persona.yaml"

# Path to the API provider configuration file #SaaS服务模式下已经不使用硬编码的yaml文件
# export MUL_IN_ONE_API_CONFIG="personas/api_configuration.yaml"


# Database connection string
export DATABASE_URL="postgresql+asyncpg://postgres:postgres@localhost:5432/mul_in_one"

# --- JWT Authentication (FastAPI-Users) ---
# 生产环境请使用强随机密钥: openssl rand -hex 32
export JWT_SECRET="your-secret-key-change-in-production"
export JWT_ALGORITHM="HS256"
export ACCESS_TOKEN_EXPIRE_MINUTES="60"

# --- OAuth Providers (可选) ---
# Gitee OAuth (墙内友好): https://gitee.com/oauth/applications
# export GITEE_CLIENT_ID="your-gitee-client-id"
# export GITEE_CLIENT_SECRET="your-gitee-client-secret"

# GitHub OAuth (国际备选): https://github.com/settings/developers
# export GITHUB_CLIENT_ID="your-github-client-id"
# export GITHUB_CLIENT_SECRET="your-github-client-secret"

# 前端地址 (OAuth 回调时使用)
export FRONTEND_URL="http://localhost:3000"

# --- SMTP 邮件服务器配置（邮箱验证功能） ---
# 如果不配置，邮箱验证功能将被禁用（但不影响注册）
# 常见配置示例：

# Gmail (需要启用两步验证并生成应用专用密码)
# export SMTP_HOST="smtp.gmail.com"
# export SMTP_PORT="587"
# export SMTP_USER="your-email@gmail.com"
# export SMTP_PASSWORD="your-16-digit-app-password"
# export SMTP_FROM_EMAIL="your-email@gmail.com"
# export SMTP_FROM_NAME="Mul-in-ONE"

# QQ邮箱 (需要生成授权码)
# export SMTP_HOST="smtp.qq.com"
# export SMTP_PORT="587"
# export SMTP_USER="your-qq@qq.com"
# export SMTP_PASSWORD="your-authorization-code"
# export SMTP_FROM_EMAIL="your-qq@qq.com"
# export SMTP_FROM_NAME="Mul-in-ONE"

# Outlook
# export SMTP_HOST="smtp-mail.outlook.com"
# export SMTP_PORT="587"
# export SMTP_USER="your-email@outlook.com"
# export SMTP_PASSWORD="your-password"
# export SMTP_FROM_EMAIL="your-email@outlook.com"
# export SMTP_FROM_NAME="Mul-in-ONE"

# --- Cloudflare Turnstile 人机验证 ---
# 如果不配置，验证码功能将被禁用（但不影响注册）
# 获取密钥: https://dash.cloudflare.com/
# 开发测试密钥（始终通过）：1x0000000000000000000000000000000AA
# export TURNSTILE_SECRET_KEY="your-turnstile-secret-key"

# --- Application Behavior Defaults ---

# Maximum number of agents that can speak in a single turn
export MUL_IN_ONE_MAX_AGENTS="2"

# Number of recent messages an agent remembers
export MUL_IN_ONE_MEMORY_WINDOW="8"

# Default temperature for model responses (creativity vs. determinism)
export MUL_IN_ONE_TEMPERATURE="0.4"
